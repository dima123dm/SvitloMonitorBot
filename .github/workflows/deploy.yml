name: Deploy Svitlo Monitor Bot

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS via SSH Password
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: root
          password: ${{ secrets.SSH_PASSWORD }}
          script: |
            cd /root/SvitloMonitorBot
            git pull origin main
            source /root/shared_env/bin/activate
            pip install -r requirements.txt
            systemctl restart SvitloMonitorBot || systemctl restart svitlo_bot
